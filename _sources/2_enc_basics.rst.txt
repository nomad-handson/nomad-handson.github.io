Basic setup
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

It is recommended to use virtual environments:

   .. code:: bash

       $ python3 -m venv <venv_name>
       $ source <venv_name>/bin/activate

Jupyter can easily be installed via pip:

   .. code:: bash

       $ pip install jupyter

The Jupyter notebook environment is launched by

   .. code:: bash

       $ jupyter notebook
